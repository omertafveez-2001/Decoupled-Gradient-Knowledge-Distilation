name: dkd_gpu_env
channels:
  - pytorch
  - defaults
  - conda-forge
dependencies:
  - python=3.8
  - pytorch=1.10.0
  - cudatoolkit=11.3  # Adjust the CUDA version based on your hardware
  - torchvision
  - torchaudio
  - timm
  - pip
  - pip:
    - matplotlib
    - scikit-learn
